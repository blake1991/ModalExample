@model ModalExample.Models.IndexVM
@{
    ViewBag.Title = "Modal Demo";
}

This is a modal demo.
<br />

<div class="row">
    <div class="col-md-3">First Name</div>
    <div class="col-md-3">Last Name</div>
    <div class="col-md-3">Address</div>
    <div class="col-md-3">Edit</div>
</div>

<hr />

@foreach (var item in Model.vols)
{
    if (item.Id != 0)
    {
        <div class="row">
            <div class="col-md-3">@item.firstName</div>
            <div class="col-md-3">@item.lastName</div>
            <div class="col-md-3">@item.address</div>
            <div class="col-md-3"><button orgId=@item.Id type="button" class="btn btn-primary editButton" data-toggle="modal" data-target="#myModal">Edit</button></div>
        </div>
    }
}
@*<div class="col-md-3"><button id="2" type="button" class="btn btn-primary editButton" data-toggle="modal" data-target="#myModal">Edit</button></div>*@


<!-- Button trigger modal -->
@*<button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#myModal">
        Launch demo modal
    </button>*@

<!-- Modal -->
<div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel">Modal title</h4>
            </div>
            <div class="modal-body">
                <!--The Div to insert returned partial into-->
                <div id="editPartial"></div>
            </div>
            @*<div class="modal-footer">

            </div>*@
        </div>
    </div>
</div>

<script type="text/javascript">





    $(".editButton").click(function () {
        console.log('clicked');
        var id = $(this).attr("orgId");
        console.log(id);
        inputData = { 'id': id }

        $.ajax(
            {
                url: '/Home/GetPartial',
                type: 'GET',
                data: inputData,
                success: function (response) {
                    $("#editPartial").html(response);
                }
            }
        );

    })

    function updateModal(data) {

        console.log(data);

    }
</script>